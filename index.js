// index.js
// Entry point for production environments where `node index.js` is executed.
// Simply requires the main application logic from app.js.
// `app.js` already connects to the database and starts the server.

require('./app');
